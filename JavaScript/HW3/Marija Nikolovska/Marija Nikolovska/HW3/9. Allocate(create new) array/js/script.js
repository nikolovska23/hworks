function allocateArray() {
    let n = prompt("Enter the length of the array");
    let a = [];
    for (i = 0; i < a.length; i++)
        a[i] = i * n;

    return a;
}

console.log(allocateArray());


// Yep!